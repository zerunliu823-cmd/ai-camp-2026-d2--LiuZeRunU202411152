# D2 starter：从真实数据到一个可检查结果

本文件只说明 starter 操作、数据契约和 TODO 边界。完整学习路线回到 `../README.md`。

## 1. 复制 starter

不要修改课程原件。复制到 `student-work/day-02-concrete`，在 VS Code 打开副本。运行 `Get-Location` 和 `Get-ChildItem`，确认当前目录与关键文件。

## 2. 下载指定真实数据

页面：https://www.kaggle.com/datasets/arunrk7/surface-crack-detection

把指定内容放到：`data/raw/Positive 与 data/raw/Negative`。不改名、不生成替代、不提交原始大数据。

## 3. 先验证

```powershell
python train.py --check-data
```

预期：REAL DATA CHECK PASSED；positive_images: 20000；negative_images: 20000。失败就停止模型路线，按 Kaggle 指南检查目录、文件名、解压层级和来源。

## 4. 安装和运行未修改起点

```powershell
python -m pip install -r requirements.txt
python train.py
```

记录基线与预期 TODO。导入错误、数据错误和文件损坏不是预期 TODO。

## 5. 学生任务

只完成 models.py 中的 SmallCNN，保持课程给定输入尺寸、输出类别和训练路线。一次只改一个小范围，不改数据、划分、指标或测试来获得漂亮结果。

```powershell
python -m unittest discover -s tests -v
python train.py
```

## 6. 结果与错误分析

检查 训练指标、混淆计数、错误图像列表或输出文件。保存基线与候选同条件比较、一个重要错误、一个不能支持的结论。任何保存数字都要能由 README 命令重新产生。

## 数据契约

- 使用者：需要先筛选大量照片、再安排人工复核的设施维护团队；
- 来源：https://www.kaggle.com/datasets/arunrk7/surface-crack-detection；
- 预期位置：`data/raw/Positive 与 data/raw/Negative`；
- 检查命令：`python train.py --check-data`；
- 使用边界：输出只用于安排人工复核，不能替代现场检查、工程师判断或安全决策；
- 主要风险：随机拆分高度相似的图像块造成泄漏；只报准确率；把筛查器当作结构安全结论。

## 提交前

README 写完整运行路线；根目录放 `report.md`、`presentation.pptx` 和 `submission.json`。运行数据检查、测试、主程序、`git diff --check`，然后按 `../08-submission.md` 建立本日独立仓库。


## 中文增补工作簿：starter

本节只解释如何安全地开始 starter。学生仍需完成 TODO；这里不会给出最终实现或作业结果。

### 学习卡 1｜第一轮：跟着材料做｜确认当前目标

**为什么现在做：** 今天的核心问题是“为维护人员制作一个初步图像筛查器，比较多数类基线与小型 CNN，并重点检查漏检裂缝”。这一小步用于把当前判断变成可以检查的动作，避免只凭智能体回答、截图或记忆宣布完成。目标过大时会让后面的数据、指标和答辩彼此脱节。

**学生操作：** 用一句不超过三十字的话重写本页问题，并圈出真实使用者、输入和输出。先自己预测会看到什么，再执行；只记录终端或文件中真实出现的内容。如果动作涉及修改，先运行 `git status`，并保证一次只改一个小目标。

**你应该得到的证据：** 一条没有写“做一个 AI”这类空话的具体问题句。证据要能指向确切文件、命令、输出、行号、图像名、时间戳或文档编号；不要补写没有实际观察到的数字。如果结果不同，保留原始报错，从“当前目录—文件是否存在—环境是否正确—命令是否完整”依次检查。

**怎样向智能体求助：** 可以说：“我正在完成 `course/zh-CN/day-02-vision/starter/README.md`。当前目标是确认当前目标。先不要修改文件，也不要给我最终答案。请根据我贴出的真实输出，解释最可能的一个原因，给我一个最小检查动作、预期结果和停止条件。”执行后把观察结果用自己的话告诉智能体，再决定下一步。

**掌握检查：** 合上智能体回答后，向同伴说明这一动作证明了什么、没有证明什么，以及如果输入、路径或条件改变，哪个结果最可能先变化。不能解释时不要让智能体替你写结论；回到本卡重新预测和运行。

**留下记录：** 在学习笔记或报告草稿写四行：`我预测`、`我执行`、`我观察`、`我决定`。只保存必要证据，不提交密钥、个人信息、下载的大数据、虚拟环境或缓存。

### 学习卡 2｜第一轮：跟着材料做｜找到权威入口

**为什么现在做：** 今天的核心问题是“为维护人员制作一个初步图像筛查器，比较多数类基线与小型 CNN，并重点检查漏检裂缝”。这一小步用于把当前判断变成可以检查的动作，避免只凭智能体回答、截图或记忆宣布完成。同时听从多个重复说明容易采用过期规则。

**学生操作：** 从课程中心进入当天目录，只把 01-problem、README、02-success-criteria 和 08-submission 当作主路线。先自己预测会看到什么，再执行；只记录终端或文件中真实出现的内容。如果动作涉及修改，先运行 `git status`，并保证一次只改一个小目标。

**你应该得到的证据：** 能说出四个文件分别负责目标、过程、证据和提交。证据要能指向确切文件、命令、输出、行号、图像名、时间戳或文档编号；不要补写没有实际观察到的数字。如果结果不同，保留原始报错，从“当前目录—文件是否存在—环境是否正确—命令是否完整”依次检查。

**怎样向智能体求助：** 可以说：“我正在完成 `course/zh-CN/day-02-vision/starter/README.md`。当前目标是找到权威入口。先不要修改文件，也不要给我最终答案。请根据我贴出的真实输出，解释最可能的一个原因，给我一个最小检查动作、预期结果和停止条件。”执行后把观察结果用自己的话告诉智能体，再决定下一步。

**掌握检查：** 合上智能体回答后，向同伴说明这一动作证明了什么、没有证明什么，以及如果输入、路径或条件改变，哪个结果最可能先变化。不能解释时不要让智能体替你写结论；回到本卡重新预测和运行。

**留下记录：** 在学习笔记或报告草稿写四行：`我预测`、`我执行`、`我观察`、`我决定`。只保存必要证据，不提交密钥、个人信息、下载的大数据、虚拟环境或缓存。

### 学习卡 3｜第一轮：跟着材料做｜确认工作目录

**为什么现在做：** 今天的核心问题是“为维护人员制作一个初步图像筛查器，比较多数类基线与小型 CNN，并重点检查漏检裂缝”。这一小步用于把当前判断变成可以检查的动作，避免只凭智能体回答、截图或记忆宣布完成。位置错误会把数据、提交或修改写到错误项目。

**学生操作：** 在 VS Code 终端运行 pwd 或 Get-Location，再运行 git status，并把输出中的仓库根目录读出来。先自己预测会看到什么，再执行；只记录终端或文件中真实出现的内容。如果动作涉及修改，先运行 `git status`，并保证一次只改一个小目标。

**你应该得到的证据：** 终端位置位于自己的工作副本，不在课程原始目录或其他日次仓库。证据要能指向确切文件、命令、输出、行号、图像名、时间戳或文档编号；不要补写没有实际观察到的数字。如果结果不同，保留原始报错，从“当前目录—文件是否存在—环境是否正确—命令是否完整”依次检查。

**怎样向智能体求助：** 可以说：“我正在完成 `course/zh-CN/day-02-vision/starter/README.md`。当前目标是确认工作目录。先不要修改文件，也不要给我最终答案。请根据我贴出的真实输出，解释最可能的一个原因，给我一个最小检查动作、预期结果和停止条件。”执行后把观察结果用自己的话告诉智能体，再决定下一步。

**掌握检查：** 合上智能体回答后，向同伴说明这一动作证明了什么、没有证明什么，以及如果输入、路径或条件改变，哪个结果最可能先变化。不能解释时不要让智能体替你写结论；回到本卡重新预测和运行。

**留下记录：** 在学习笔记或报告草稿写四行：`我预测`、`我执行`、`我观察`、`我决定`。只保存必要证据，不提交密钥、个人信息、下载的大数据、虚拟环境或缓存。

### 学习卡 4｜第一轮：跟着材料做｜先读再改

**为什么现在做：** 今天的核心问题是“为维护人员制作一个初步图像筛查器，比较多数类基线与小型 CNN，并重点检查漏检裂缝”。这一小步用于把当前判断变成可以检查的动作，避免只凭智能体回答、截图或记忆宣布完成。未读仓库就生成代码常会猜错文件名和接口。

**学生操作：** 请智能体只读本页和相关代码，列出它看到的文件、TODO、测试和不能改动的边界。先自己预测会看到什么，再执行；只记录终端或文件中真实出现的内容。如果动作涉及修改，先运行 `git status`，并保证一次只改一个小目标。

**你应该得到的证据：** 计划中的每一个路径都真实存在，且没有产生文件修改。证据要能指向确切文件、命令、输出、行号、图像名、时间戳或文档编号；不要补写没有实际观察到的数字。如果结果不同，保留原始报错，从“当前目录—文件是否存在—环境是否正确—命令是否完整”依次检查。

**怎样向智能体求助：** 可以说：“我正在完成 `course/zh-CN/day-02-vision/starter/README.md`。当前目标是先读再改。先不要修改文件，也不要给我最终答案。请根据我贴出的真实输出，解释最可能的一个原因，给我一个最小检查动作、预期结果和停止条件。”执行后把观察结果用自己的话告诉智能体，再决定下一步。

**掌握检查：** 合上智能体回答后，向同伴说明这一动作证明了什么、没有证明什么，以及如果输入、路径或条件改变，哪个结果最可能先变化。不能解释时不要让智能体替你写结论；回到本卡重新预测和运行。

**留下记录：** 在学习笔记或报告草稿写四行：`我预测`、`我执行`、`我观察`、`我决定`。只保存必要证据，不提交密钥、个人信息、下载的大数据、虚拟环境或缓存。
