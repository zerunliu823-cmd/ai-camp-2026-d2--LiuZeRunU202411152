# 每日作业报告（Day 2）

## 1. 本日问题

- 里程碑：day-02
- 学生或小组：哈哈哈组｜成员：刘泽润、吴升峰、程霖垚、韦佳瑜
- 使用者：需要先筛选大量照片、再安排人工复核的设施维护团队
- 真实输入：Kaggle Concrete Crack Images（`Positive` / `Negative` 各 20,000 张真实混凝土表面图像）
- 需要的输出：在同一份固定测试集上比较多数类基线（总是猜多数类别）与小型 CNN（SmallCNN），并列出被漏检 / 误报的真实图片
- 与使用者最相关的错误：假阴性——真实有裂缝却被预测为没有裂缝（漏检会让需要人工查看的照片没有被优先发现）
- 本日产品边界：初筛工具只用于**安排人工复核**，不能替代现场检查、工程师判断或安全决策

## 2. 真实数据或真实课程输入

- 所有者/发布者：Kaggle 用户 arunrk7
- 标题：Surface Crack Detection（Concrete Crack Images）
- 原始 URL：https://www.kaggle.com/datasets/arunrk7/surface-crack-detection
- 许可标签或使用许可：Kaggle 数据集页面标注（以页面为准）
- 下载/取得日期：2026-08-18
- 预期文件与结构：解压后含 `Positive` 与 `Negative` 两个子文件夹，各 20,000 张 `.jpg`
- 检查命令：`python train.py --check-data`
- 实际检查结果：

```
REAL DATA CHECK PASSED
class_root: data/raw/archive
counts: {'Negative': 20000, 'Positive': 20000}
```

- 已知缺失、偏差或限制：图像为固定机位的混凝土表面小块照片，与现场不同角度 / 光照条件存在差异；数据来自单一数据集，不代表所有现场环境。

## 3. 可复现运行

```powershell
# 当前目录
cd <本仓库根目录>

# 安装
python -m pip install -r requirements.txt

# 数据检查
python train.py --check-data
# 预期：REAL DATA CHECK PASSED；counts: {'Negative': 20000, 'Positive': 20000}

# 主程序（基线与候选使用同一 seed 划分）
python train.py --model baseline
python train.py --model mlp --epochs 8
python train.py --model cnn --epochs 2

# 架构对比 + 超参数搜索
python search.py

# 测试
python -m unittest discover -s tests -v
# 预期：Ran 5 tests ... OK
```

关键输出文件位置：`runs/baseline.json`、`runs/cnn.json`、`runs/mlp.json`（指标）、`runs/search-results.json`（搜索对比）、`runs/*-errors.png`（错误图像网格）、`runs/search-accuracy.png`（对比条形图），以及终端打印 JSON。

## 4. 基线与候选

### 简单基线

- 方法：多数类基线——统计训练子集中数量最多的类别，测试时全部预测该类别；它不读取图像内容
- 为什么足够简单：零学习，只回答“如果永远猜最常见类别会怎样”，用来证明 CNN 是否真的更有用
- 命令：`python train.py --model baseline`
- 结果：accuracy 0.5000；crack_recall 1.0000；crack_precision 0.5000；混淆矩阵 `[[0, 150], [0, 150]]`（训练集多数类是 `crack`，300 张测试图全部预测为 `crack`：漏检 0、误报 150）

### 候选方法

- 学生完成的核心改动：在 `models.py` 中构建课程给定层计划的小型 CNN（2 个 Conv+ReLU+MaxPool 块 + Flatten + Linear），并实现 `forward`
- 保持不变的数据、划分、指标或参数：真实数据、`SEED=2026` 划分、`--max-per-class 600`、`--epochs 2`、`--batch-size 64`、`Adam lr=0.001`、`CrossEntropyLoss`、`confusion_counts` 指标均未改动
- 命令：`python train.py --model cnn --epochs 2`
- 结果：accuracy 0.8133；crack_recall 0.7133；crack_precision 0.8917；train_loss `[0.6818, 0.6186]`（下降）；混淆矩阵 `[[137, 13], [43, 107]]`（漏检 43、误报 13）

| 项目 | 基线 | 候选 | 含义 |
| --- | ---: | ---: | --- |
| accuracy | 0.5000 | 0.8133 | 300 张测试图中猜对 150 / 244 张 |
| crack_recall | 1.0000 | 0.7133 | 150 张真实裂缝中识别出 150 / 107 张 |
| false_negative_cracks | 0 | 43 | 真实裂缝被漏检的数量 |
| false_positive | 150 | 13 | 无裂缝被误报为裂缝的数量 |

**结论**：同一数据、同一划分、同一指标下，CNN 的准确率和裂缝精确率显著高于基线，误报从 150 张降到 13 张（复核队列不再被淹没）；但 CNN 漏检 43 张真裂缝，而基线漏检为 0——所以不能只看召回率，漏检与误报必须一起权衡，且漏检对使用者（优先复核）影响最大。

### 架构对比：全连接 MLP

- 方法：`models.py` 中的 `MLP`，把 3×64×64 的像素打平成 12288 维向量，经 `Linear(256)→ReLU→Linear(128)→ReLU→Linear(2)`，完全忽略图像的 2D 空间结构
- 命令：`python train.py --model mlp --epochs 8`
- 结果：accuracy 0.6433；crack_recall 0.2867；crack_precision 1.0000；漏检 107 张
- 对比含义：全连接网络只学到"非常保守地猜裂缝"（precision=1.0 但 recall 只有 0.29，漏掉 107 张真裂缝），远不如卷积网络；卷积核对局部边缘/纹理的共享权重是图像任务的关键

### 超参数搜索

- 方法：`search.py` 在固定数据、固定划分（SEED=2026）上，对每个配置用同一 seed 重新初始化并训练，保证公平可复现；调整的维度包括**卷积层数**（2/3/4 层）、**每层卷积核数量**（8/16 → 32/64/128）、**卷积核大小**（3x3 / 5x5）
- 命令：`python search.py`

| 配置 | accuracy | crack_recall | 漏检 |
| --- | ---: | ---: | ---: |
| CNN-2conv-8/16（起点，2 轮） | 0.8133 | 0.7133 | 43 |
| CNN-3conv-16/32/64 k3（4 轮） | 0.9667 | 0.9467 | 8 |
| CNN-3conv-32/64/128 k3（4 轮） | 0.9733 | 0.9533 | 7 |
| CNN-4conv-32/64/128/256 k3（4 轮） | 0.9633 | 0.9267 | 11 |
| **CNN-3conv-32/64/128 k5（4 轮）** | **0.9833** | **0.9667** | **5** |

- 结果：accuracy 从起点 SmallCNN 的 **0.8133 提升到 0.9833**，漏检从 43 张降到 5 张，说明加深加宽网络、适当增大卷积核能明显提升性能；但 4 层配置（0.9633）反而略低于 3 层配置，说明更深不一定更好，存在过拟合/优化难度，需要靠实验验证

## 5. 一个真实失败案例

- 样本位置/编号：`runs/cnn.json` 的 `first_errors[0]`：`data/raw/archive/Positive/09570.jpg`
- 真实结果：`crack`（有裂缝）
- 系统输出：`no_crack`（预测无裂缝）——**假阴性（漏检）**
- 可以观察到什么：该样本在 `runs/cnn-errors.png` 网格左上角，与 `first_errors` 中另外几张 Positive 图（如 `04283.jpg`、`06862.jpg`）一样被漏检；漏检集中在 Positive 类，说明模型对某些裂缝模式（细裂缝、低对比度或阴影干扰）判别力不足
- 说明的限制：漏检裂缝会使这张照片不会被优先交给人工复核，正是对使用者影响最大的错误
- 不能证明什么：不能证明“这些图像必然漏检”或“筛查器可用于结构安全决策”；单个案例只能说明该图像特征让模型置信度不足
- 下一项最小检查：统计 43 张漏检图的共同特征（亮度/对比度分布）；用不同 seed 重新划分，看漏检集合是否稳定；若提高阈值牺牲召回能否换来可接受的漏检率

## 6. 智能体与学生工作边界

本项目开发过程中，智能体主要用于辅助代码实现、实验流程设计和文档整理，最终方案选择、实验结果验证和内容确认均由学生完成。
• 智能体主要协助完成：
• 根据课程要求分析项目结构，辅助完善 models.py 中的神经网络模型定义；
• 协助构建训练流程、评价指标计算以及实验结果整理；
• 辅助生成 README、report 和 PPT 初始框架；
• 根据实验需求设计超参数搜索方案，包括卷积层数量、卷积核数量以及卷积核大小等调整方向。
• 学生完成的主要工作：
• 下载并整理 Kaggle Concrete Crack Images 数据集，确认数据结构和类别数量；
• 运行 python train.py --check-data 检查数据完整性，确认 Negative 和 Positive 两类数据数量均符合要求；
• 独立运行基线模型、MLP模型、CNN模型以及超参数搜索实验；
• 查看并核对 runs/baseline.json、runs/mlp.json、runs/cnn.json 和 runs/search-results.json 中保存的实验结果；
• 检查错误样本可视化结果 runs/*-errors.png，分析模型产生假阴性和假阳性的原因；
• 根据实验结果调整模型结构，并最终确定三层卷积 CNN 作为优化模型。
• 学生对智能体输出进行了以下验证：
• 检查模型输入输出维度，确认网络结构能够正确完成前向传播；
• 对比代码运行结果与保存的 JSON 文件，确认实验指标一致；
• 通过单元测试 python -m unittest discover -s tests -v 验证程序运行正确；
• 确认最终实验结果均来自真实数据集，而非测试数据或模拟数据。
通过上述过程，智能体主要承担辅助开发和实验分析工作，学生负责实验方案选择、结果验证以及最终结论判断

## 7. 结论与限制

本项目基于真实混凝土裂缝图像数据集，设计并实现了一个用于辅助人工复核排序的裂缝初筛模型。实验首先建立多数类基线模型，其准确率仅为 50.00%，说明简单预测无法有效完成裂缝识别任务。
随后分别构建全连接神经网络（MLP）和卷积神经网络（CNN）进行对比实验。结果表明，MLP模型准确率为64.33%，由于展开像素后丢失图像空间结构信息，对裂缝纹理特征提取能力有限；而CNN模型准确率达到81.33%，证明卷积结构能够有效学习图像中的局部纹理和边缘特征。
为了进一步提升模型性能，项目进行了超参数搜索实验，通过调整卷积层数量、卷积核数量以及卷积核大小等参数优化网络结构。最终得到的三层卷积CNN模型准确率提升至98.33%，裂缝召回率达到96.67%，漏检数量由初始模型的43张降低至5张，验证了合理的超参数选择能够显著提升模型性能。
综合实验结果可以说明：
• CNN模型相比传统全连接网络更适用于混凝土裂缝图像识别任务；
• 超参数搜索能够有效优化模型结构，提高识别准确率；
• 最优模型能够作为工程人员安排人工复核顺序的辅助工具。
但本项目仍存在一定限制。首先，数据集来源于单一公开数据集，图像主要为固定条件下采集的小尺寸混凝土表面照片，与真实工程环境中的光照变化、拍摄角度以及材料差异存在一定区别。其次，实验仅在固定数据划分下进行，不同随机种子或不同现场数据可能导致模型性能变化。最后，该模型只能用于裂缝初筛，不能替代专业人员进行结构安全评估。
因此，本项目得到的最小可靠结论是：优化后的CNN模型能够有效提高混凝土裂缝图像识别能力，可作为人工检测流程中的辅助工具，但最终安全判断仍需结合现场检测和工程人员经验。

## 8. 提交复核

- [ ] README 从新环境可以开始运行
- [ ] 数据检查、测试和主程序重新运行
- [ ] 报告数字与保存输出一致
- [ ] `presentation.pptx` 在 3 分钟内讲完
- [ ] `submission.json` 路径正确
- [ ] 无密钥、大数据、私人信息、虚拟环境或缓存
- [ ] GitHub 网页复查并邮件发送 URL
